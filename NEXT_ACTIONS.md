# Next Actions

1. Redeploy polished Streamlit (`streamlit_app.py` Reader/Evaluation pages) to Cloud Run UI.
2. Expand evaluation datasets to ≥30 retrieval and ≥25 QA examples with verified pages/chunks from ingested papers.
3. Capture screenshots under `docs/screenshots/`.
4. Finish `ARCHITECTURE.md` + `FINAL_PROJECT_REPORT.md` portfolio polish.
5. Optional: configure real embedding provider + LangSmith when keys are intentionally enabled.
